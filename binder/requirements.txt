# Binder builds ONE environment for the whole BenC-Sec/Tools repo. Rather than
# duplicating each tool's dependencies here, aggregate the tools' own
# requirements files so each tool's list stays the single source of truth.
#
# To add a new tool: drop a `<NewTool>/requirements.txt` in its folder and add
# one line below. Keep an eye out for version conflicts between tools — they all
# share this one environment.
#
# pip resolves these -r paths relative to THIS file (the binder/ folder).

-r ../Timeline_creator/requirements.txt
# -r ../NextTool/requirements.txt
